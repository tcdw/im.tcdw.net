---
interface Props {
  title: string;
  tone?: 'primary' | 'accent';
}

const { title, tone = 'primary' } = Astro.props;
---

<h2 class="flex items-center gap-3">
  <span class="text-(length:--fs-title) font-semibold tracking-tight text-secondary-900">
    {title}
  </span>
</h2>
